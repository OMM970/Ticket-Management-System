spring:
  application:
    name: booking-service

  data:
    mongodb:
      host: localhost
      port: 27017
      database: booking_db

  redis:
    host: localhost
    port: 6379
    timeout: 2000000

  cache:
    type: redis

logging:
  level:
    root: INFO
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG
    org.springframework.data.redis: DEBUG

server:
  port: 2019


jwt:
  payment:
    secret: 120a815e74a9edf768ac8db1bf0de298ab694cf9b922afc45551eac4e0e7fd7a
    expiry-minutes: 10

inventory:
  service:
    name: ${INVENTORY_SERVICE_NAME:inventory-service}
    url: ${INVENTORY_SERVICE_URL:http://localhost:9090}
